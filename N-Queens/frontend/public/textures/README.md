# Sci-Fi Tactical Room Background

Place your background texture image here (e.g., `tactical-room.jpg`) for the parallax background layer. The `ParallaxBackground` component applies it with gradient overlays and a subtle grid pattern.
